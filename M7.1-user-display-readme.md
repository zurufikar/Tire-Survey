M7.1 patch: human-readable user identity in PM/PIC reports.

Replace only:
- app/reports/page.tsx

Run this migration in Supabase:
- supabase/migrations/019_pm_pic_report_users.sql

The database keeps UUIDs as internal identifiers. PM/PIC now sees User Code + Full Name in report history and can search by User Code or name.
